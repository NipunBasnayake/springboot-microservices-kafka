spring:
  application:
    name: product

  datasource:
    url: jdbc:mysql://localhost:3306/product_kafka_db?createDatabaseIfNotExist=true
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update

  output:
    ansi:
      enabled: always